services:
  - type: web
    name: moysklad-production-mcp
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MOYSKLAD_TOKEN
        sync: false
